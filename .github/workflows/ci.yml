name: CI

on:
  pull_request:
  push:
    branches: [dev]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26 # the version the build was verified on
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
